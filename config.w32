// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("fzip", "for fzip support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("fzip", "enable fzip support", "no");

if (PHP_FZIP != "no") {
	EXTENSION("fzip", "fzip.c");
}

